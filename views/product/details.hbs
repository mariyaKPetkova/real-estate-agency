{{!-- <section id="viewhotelDetails">
    <h2>Details</h2>
    <div class="hotel-ticket">
        <div class="hotel-left">
            <img src="{{product.imageUrl}}" alt="">
        </div>
        <div class="hotel-right">
            <div>
                <h3>{{product.name}}</h3>
            </div>
            <div>
                {{product.city}}
            </div>
            <p><span>Free rooms: {{product.rooms}}</span> </p>
            {{#if product.hasUser}}
            {{#if product.isAuthor}}
            <a href="/products/edit/{{product._id}}" class="edit">Edit</a>
            <a href="/products/delete/{{product._id}}" class="remove">Delete</a>
            {{else}}
            {{#if product.isBooked}}
            <p><span class="green">You already have booked a room</span> </p>
            {{else}}
            <a href="/products/book/{{product._id}}" class="book">Book</a>
            {{/if}}
            {{/if}}
            {{/if}}
        </div>
    </div>
</section> --}}


{{!--
<section id="details-page">

    <div class="main_card">
        <div class="card_left">
            <div class="card_datails">

                <h1>Title: {{product.name}}</h1>
                <h3>Created by an author: {{product.nameAuthor}}</h3>
                <div class="card_animal">
                    <p class="card-keyword">Keyword: {{product.keyword}}</p>
                    <p class="card-location">Location: {{product.city}}</p>
                    <p class="card-date">Date: {{product.date}}</p>
                </div>

                <p class="disc">Description: {{product.description}}</p>

                <!-- If there is no registered user, do not display buttons-->
                <div class="social-btn">
                    <!-- Only for registered user and author of the post -->

                    {{#if product.isAuthor}}
                    <a href="/products/edit/{{product._id}}" class="edit-btn">Edit</a>
                    <a href="/products/delete/{{product._id}}" class="del-btn">Delete</a>
                    <!-- logged in users, who have not yet voted-->
                    {{/if}}
                    {{#if product.isntAuthor}}
                    {{#if product.isVoted}}
                    <p class="thanks-for-vote">Thanks For Voting</p>
                    {{else}}
                    <a href="/products/vote-up/{{product._id}}" class="vote-up">UpVote +1</a>
                    <a href="/products/vote-down/{{product._id}}" class="vote-down">DownVote -1</a>
                    {{/if}}
                    {{/if}}


                </div>
            </div>
        </div>
        <div class="card_right">
            <img src="{{product.imageUrl}}" alt="image">
        </div>
    </div>

</section>

<section id="votes">
    <div class="vote-info">
        <div class="card_left">
            <div class="card_datails">
                <h1>Votes</h1>
                <div class="card_vote">
                    <p class="PV">Total rating of votes: {{product.vote}}</p>
                </div>
                {{#if product.hasVote}}
                <p class="disc">People who voted for the post - {{product.voters}}</p>
                {{else}}
                <p class="disc">People who voted for the post - No one has voted yet.</p>
                {{/if}}
            </div>
        </div>
    </div>
</section> --}}

<section id="deatils-page">
    <div class="wrapper">
        <div class="product-img">
            <img src="./static/images/apartments.jpg">
        </div>
        <div class="product-info">
            <div class="product-text">
                <div class="product-text">
                    <h1>Name: {{product.name}}</h1>
                    <h2>Type: {{product.type}}</h2>
                    <h4>Year: {{product.year}}</h4>
                    <h4>City: {{product.city}}</h4>
                    <p>Description: {{product.description}}</p>

                    <!-- if there are already tenants of the housing, separate their names with a comma and a space ", "  -->
                    
                    {{#if product.isAuthor}}
                    <a href="/products/edit/{{product._id}}" class="edit">Edit</a>
                    <a href="/products/delete/{{product._id}}" class="remove">Delete</a>
                    {{/if}}

                    
                    {{#if product.hasTenants}}
                    <p>People rented this housing: {{product.tenants}}</p>
                    {{else}}
                    <p>People rented this housing: There are no tenants yet.</p>
                    {{/if}}
                
                </div>
            </div>
            
            <div class="product-btn">
                {{#if product.isntAuthor}}
                {{#if product.alreadyRent}}
                <p class="alRentHome">You have already rent this home</p>
                {{else}}
                {{#if product.free}}
                <a href="/products/rent/{{product._id}}" class="rentHome">Rent a home, available {{product.availablePieces}} housing</a>
                {{else}}
                <p class="no-housing">No Housing Available!</p>
                {{/if}}
                {{/if}}
                {{/if}}
            </div>
            
            
        </div>
    </div>
</section>